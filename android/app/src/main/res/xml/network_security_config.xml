<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext HTTP is off by default from API 28 onward, which would block the
     loopback server the web bundle is served from. Re-enable it for loopback only;
     every other destination stays TLS-only. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
